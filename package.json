{
  "name": "route-delivery-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm run prisma:generate --prefix backend && npm run prisma:migrate:deploy --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
